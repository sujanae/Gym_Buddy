import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF1D1A49);
  static const Color primary = Color(0xFFF39F5A);
  static const Color accent = Color(0xFFF39F5A); // same as primary
  static const Color secondary = Color(0xFF292C6D); // optional card background
  static const Color text = Colors.white;
}
